#include<stdio.h>
#include<stdlib.h>

struct slink{
    int data;
    struct slink* next;
};

void main()
{
    struct slink* start = NULL;

    struct slink* newnode = (struct slink*)malloc(sizeof(struct slink));


}